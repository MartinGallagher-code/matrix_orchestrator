include LICENSE
include README.md
include CHANGELOG.md
include REUSE.toml
recursive-include LICENSES *
# Ship the test suite in the sdist so distro packagers and auditors can run
# it against exactly what they unpacked. It needs only bash and python3.
recursive-include tests *
